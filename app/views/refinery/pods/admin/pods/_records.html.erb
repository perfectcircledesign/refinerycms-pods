<%= render 'refinery/admin/search_header', :url => refinery.pods_admin_pods_path %>
<div class='pagination_container'>
  <% if @pods.any? %>
    <%= render 'pods' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
